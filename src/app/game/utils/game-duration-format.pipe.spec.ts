import { GameDurationFormatPipe } from './game-duration-format.pipe';


describe('GameDurationFormatPipe', () => {

  it('create an instance', () => {
    const pipe = new GameDurationFormatPipe();
    expect(pipe).toBeTruthy();
  });

});
